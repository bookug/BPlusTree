/* this program is used for test! */
//we can do lots of tests with debug-system

#include "tree/Tree.h"
#include "storage/Storage.h"
#include "util/Util.h"
using namespace std;

int 
main(int argc, char **argv)
{
	printf("This is a B+ tree!\n");
	//Tree t;
	//printf("size of Node: %u\n", (unsigned)sizeof(Node));
	//printf("size of IntlNode: %u\n", (unsigned)sizeof(IntlNode));
	//printf("size of LeafNode: %u\n", (unsigned)sizeof(LeafNode));
	return 0;
}
